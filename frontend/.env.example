# ---- frontend/.env (dev) ----
# Local dev uses the proxy in package.json (http://localhost:8000) — no var
# needed. Set this in your Vercel project env for production builds.

REACT_APP_BACKEND_PROD_URL=https://your-render-backend.onrender.com
